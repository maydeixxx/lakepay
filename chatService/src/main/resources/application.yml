server:
  port: 8082

spring:
  datasource:
    # Change db credentials before merging to dev
    url: jdbc:postgresql://localhost:5432/chat_db
    username: postgres
    password: secret
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
      database: postgresql
      database-platform: org.hibernate.dialect.PostgreSQLDialect

token:
  signing:
    key: your-secure-signing-key-here