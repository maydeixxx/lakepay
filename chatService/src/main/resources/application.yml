server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://postgres-db:5432/chat_db
    username: postgres
    password: secret
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
      database: postgresql
      database-platform: org.hibernate.dialect.PostgreSQLDialect